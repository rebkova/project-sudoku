{
  "name": "project-sudoku-parent",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm install && node --experimental-modules server.js",
    "postinstall": "npm install --prefix backend"
  }
}